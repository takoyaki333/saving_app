<h1><%= current_user.name %>さんの目標一覧</h1>

<% if @goals.any? %>
  <ul>
    <% @goals.each do |goal| %>
      <li>
        <%= link_to goal.name, goal_path(goal) %>（目標金額: <%= goal.target_amount %>円 / 現在: <%= goal.saved_amount || 0 %>円）
      </li>
      <%= link_to "目標を作成する", new_goal_path, class: "btn btn-primary" %>
    <% end %>
  </ul>
<% else %>
  <p>まだ目標がありません</p>
  <%= link_to "目標を作成する", new_goal_path, class: "btn btn-primary" %>
<% end %>
